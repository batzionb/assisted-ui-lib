import React from 'react';
import { FormGroup } from '@patternfly/react-core';

export function SubnetRange() {
  return (
    <FormGroup isRequired label="Subnet range" name="subnetRange" fieldId="form-subnetRange-field">
      placeholder
    </FormGroup>
  );
}
